![Build Status](https://github.com/RnCode8/PlaywrightTests/actions/workflows/playwright-tests.yml/badge.svg)


Write a several paragraph reection comparing:
Manual UI testing: You wrote it in Java + Playwright
AI-assisted UI testing: You prompted the MCP agent to generate Playwright
tests. You can compare aspects like:
Ease of writing and running tests
Accuracy and reliability of generated tests
Maintenance effort
Limitations or issues you encountered

# Paragraph reflection

This section of the project was Way harder and way more time consuming than the github actions part of the assignment. Lots of the data is old such as the pricing had to be edited. Then tons of errors using playwright
I would say that I had to run about 50 tests to get it to run. Then I had to edit it about another 10 times to get github actions to pass. This section is rough.
I feel like I learned things but it's quite the hard project.

As for the MCP agent, honestly I think it's just as bad as a person writing the playwright code. It struggled as much as I did writing the code because of how strict playwright could be
The ease of writing was definitely easier because well I wasn't the one thinking about what I neede where but the running of the tests were close to just as bad as mine.
I had to go through each error and later one the agent would write code that made the same assert error as before. So for accuracy and reliability of the test I would say it's the same as a normal persons.
The limitation I encoutered would be just how much time it took and the amount of double checks it asks for even with deeper and longer prompts.

The last thing I want to tocuh up on is the maintenance effort. I'd say if the bookstore website had an update, there'd probably be tons of issues the agent would run into again.
It's just not that great at using playwright I guess? Or maybe playwright is too specific and I wasn't giving it good enough prompts. But in my opinion maintenance on the code
useing the agent would probably be not great and it would take multiple iterations just like the first time around to create a updated and well maintained test.
